# Secrets — the real quadlet holds plaintext credentials; never commit it
chipanel.container

# Build artifacts
backend/target/
frontend/node_modules/
frontend/build/
frontend/.svelte-kit/

# Local/agent tooling & ad-hoc artifacts (not project source)
.agents/
.claude/
.playwright-mcp/
audit-2026-08-08/
bento_prototype.html

# OS/editor noise
.DS_Store
*.swp
